After running the code a GUI will appear 
first we need to select a box for start node (indicated by a yellow box)
then the second click for the goal node (indicated by a blue box)

then define the obstacles by clicking on the left over boxes (indicates by black boxes)

then press the space button to start the game